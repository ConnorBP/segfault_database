#puts the daemon into the proper location with the proper configuration values
#root is required to run this

# Install on ubuntu the required library for talking to mysql
sudo apt-get install libmariadb-dev